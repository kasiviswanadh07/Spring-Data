spring:
  config:
    activate:
      on-profile: test

transliteration:
  useDummy: true

logging:
  level:
    root: WARN

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
